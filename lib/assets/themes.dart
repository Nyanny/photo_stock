import 'package:flutter/material.dart';

/// Class [AppTheme] defines themes constants in the app
class AppTheme {
  static final appTheme = ThemeData(
    primarySwatch: Colors.blue,
    appBarTheme: AppBarTheme(backgroundColor: Colors.grey[50]),
  );
}
